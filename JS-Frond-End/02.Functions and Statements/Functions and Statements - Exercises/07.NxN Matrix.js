function nMatrix(num){
    for (let row = 0; row < num; row++) {
        let matrix = [];
        for (let col = 0; col < num; col++) {
        matrix.push(num)
        }
        console.log(matrix.join(' '));
    }
}
nMatrix(3);